# Chief Of Staff Workboard

This file is *not* the canonical live board.

Use it only for:

- quick capture before triage
- migration notes while moving work into `Linear`
- short-lived review scratch notes

If an item becomes real work, put it in the right system:

- `Linear` for active work and follow-up
- `Google Calendar` for actual commitments
- `Claw Fitness` / `Train` for workout execution and logging
- local docs for durable preferences or playbooks

## Migration Queue

- `COS-001` `goal` `inbox` `user + andy`
  outcome: Lock the user's top goals for 2026 and define what winning looks
  like in each major area.
  next_action: Capture goals and turn the active ones into `Linear` issues or
  durable profile updates.

- `COS-002` `project` `inbox` `user + andy`
  outcome: Fill `PROFILE.md` with real preferences for dating, travel, gifts,
  purchases, budget, and approvals.
  next_action: Gather baseline preferences and update local docs.

- `COS-003` `ops` `ready` `andy`
  outcome: Stand up recurring chief-of-staff reviews without creating duplicate
  state.
  next_action: Create recurring `Linear` issues for daily review, inbox
  triage, and monthly financial review.

- `COS-004` `decision` `waiting_on_user` `user`
  outcome: Set initial approval thresholds for purchases, travel, gifts, and
  sensitive outreach.
  next_action: User defines default approval caps and no-go actions, then
  update `PROFILE.md`.

- `COS-005` `project` `waiting_on_user` `user + andy`
  outcome: Seed the important people, birthdays, trips, and recurring social
  commitments worth tracking.
  next_action: Create first-pass people and events list, then schedule the
  relevant follow-ups in `Linear` and `Google Calendar`.

## Scratch Notes

- Keep this section disposable.
- Delete notes once they have been triaged into the proper system.
